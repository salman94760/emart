export default function AdminAuthSubSideBar({ title, title1 }) {
  return (
    <div>
      <h2 className="text-4xl font-bold">{title}</h2>
      <p className="text-gray-400 text-sm mt-2">{title1}</p>
    </div>
  );
}
